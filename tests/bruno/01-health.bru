meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{appUrl}}/api/health
  body: none
  auth: none
}

docs {
  Process liveness check.

  ## Input

  No parameters, headers, or body.

  ## Response

  - `200 OK`: `status` = `ok`.
}

tests {
  test("is healthy", function() {
    expect(res.status).to.equal(200);
    expect(res.body.status).to.equal("ok");
  });
}
