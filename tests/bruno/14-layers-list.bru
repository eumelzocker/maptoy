meta {
  name: Layers - List
  type: http
  seq: 14
}

get {
  url: {{appUrl}}/api/layers
  body: none
  auth: none
}

docs {
  Lists the global, Map-Set-independent overlay stack without changing state.
}

tests {
  test("lists layers", function() {
    expect(res.status).to.equal(200);
    expect(res.body.items).to.be.an("array");
  });
}
